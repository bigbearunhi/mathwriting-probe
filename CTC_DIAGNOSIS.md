# CTC 空白输出排查（2026-09-18）

原模型及 3000 步存档未改动。测试集未使用。

## 检查
- 10 条原始 train InkML 重新解析，与缓存标签、特征字节一致。
- 3 项现有单元测试通过：CTC 重复/空白、曲线相对位置和时间、padding 隔离及有限梯度。
- 固定随机种子与相同 8 条训练样本、相同模型、初始化及 dropout，只改变学习率。
- lr=0.001：400 步训练样本 EM 3/8，Token 错误率 14.43%。
- lr=0.0001：200 步 EM 8/8；400 步仍为 8/8，Token 错误率 0%。这是记忆能力检查，不是泛化准确率。

## 完整数据对照
从相同 seed=42 重新初始化，使用相同训练采样顺序、有效 batch256、microbatch8、512 条固定验证集，只改变学习率到 0.0001。运行 300 步后结束。

|300 步指标|原 lr=0.001|lr=0.0001|
|---|---:|---:|
|ctc_loss|3.994119|3.699198|
|token_error_rate|0.999590|0.872369|
|exact_match|0.000000|0.000000|
|blank_fraction|0.999837|0.962075|

## 结论与限制
较小学习率在单次固定种子对照中明显改善了训练样本拟合及早期验证指标。原学习率与当前实现组合的优化问题是重要线索，尚不能认定为唯一根因。模型能学会这 8 条样本，不代表整个预处理、标签或结构没有问题。新模型在验证集仍为 0/512 完整正确，主要输出括号等常见 token，不能声称已学会有效识别。下一步应在较小学习率下延长受控试验，持续看验证 Token 错误率、完整准确率和实际输出；若停滞，再单独比较预处理与位置表示等因素。不要直接投入 10 万步。

Google 论文确实写了 Adam lr=1e-3（https://arxiv.org/html/2404.10690v2 第5.2节），本次结果不表示论文参数错误：本地实现及特征处理并非完整官方实现。

脚本：diagnose_ctc.py、diagnose_full_ctc.py。
结果：runs/ctc-diagnosis/、runs/ctc-full-lr-diagnosis/。

## 后续对照：低学习率续训至600步
保持模型、优化器和随机状态，从300步续训。512条验证集：400步 Token错误率82.73%，600步81.12%，600步loss3.1195，EM仍为0/512。
128条验证样本的输入清零检查：真实输入loss3.0279、Token错误率80.21%；清零输入（保留长度）loss10.0830、Token错误率83.49%。这是输入依赖的证据，但清零属于分布外输入且错误率差距有限，不能据此宣称符号识别成功。
已启动 mathwriting-ctc-low-lr.service，从600续训至3000，每100步验证和保存。当前活跃低学习率进度以 runs/ctc-lr-continuation.log 和 runs/ctc-full-lr-diagnosis/validation_*.json 为准，旧 runs/ctc-full/status.json 仍是旧学习率结果。
